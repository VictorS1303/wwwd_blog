---
const { accordionItems } = Astro.props

import Section from "../generals/section/Section.astro"
import AccordionItemsContainer from "../accordion/AccordionItemsContainer.astro"

import { fetchAccordianData } from "../../../js/utils/supabase_methods"

const accodianData = await fetchAccordianData()
---

<Section class="about-section mt-8" id="about_section">
  <div class="flex flex-col gap-4 md:flex-row md:justify-between">
    <AccordionItemsContainer class="w-full" accordionItems={accodianData} />
  </div>
</Section>
