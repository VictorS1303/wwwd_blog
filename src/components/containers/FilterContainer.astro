---
const { class: className, id: id } = Astro.props
import TabsContainer from "../tabs/TabsContainer.astro"
import Input from "../input/Input.astro"
---

<div class=`${className} flex flex-col items-start justify-between gap-6 md:flex-row md:gap-0` id={id}>
  <Input
    class="text-2xl"
    id="blog_post_filter"
    placeholder="Search posts by title"
  />
  <TabsContainer />
</div>
